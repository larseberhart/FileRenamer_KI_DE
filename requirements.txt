pypdf>=4.2.0,<6.0.0

# Optionale Abhängigkeiten für OCR-Fallback (gescannte PDFs ohne eingebetteten Text).
# Zusätzlich werden die Systemtools Tesseract und Poppler benötigt:
#   brew install tesseract tesseract-lang poppler
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
